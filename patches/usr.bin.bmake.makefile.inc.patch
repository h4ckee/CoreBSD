diff -Naur src.orig/usr.bin/bmake/Makefile.inc src/usr.bin/bmake/Makefile.inc
--- src.orig/usr.bin/bmake/Makefile.inc	2026-06-12 10:36:48.000000000 +0400
+++ src/usr.bin/bmake/Makefile.inc	2026-06-18 16:24:05.071163000 +0400
@@ -10,12 +10,10 @@
 .export SRCTOP
 .endif
 
-.if exists(${.CURDIR}/tests)
 PROG= make
 .if ${PROGNAME:U} != "bmake"
 LINKS= ${BINDIR}/make ${BINDIR}/bmake
 MLINKS= ${MAN} b${MAN}
-.endif
 .endif
 
 # hack to not add tests to tests subdir since this is included from
