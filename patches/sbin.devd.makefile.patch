diff -Naur src.orig/sbin/devd/Makefile src/sbin/devd/Makefile
--- src.orig/sbin/devd/Makefile	2026-06-12 10:36:43.000000000 +0400
+++ src/sbin/devd/Makefile	2026-06-17 14:54:07.459264000 +0400
@@ -6,9 +6,6 @@
 CONFS=	devd.conf
 DEVD=	devmatch.conf
 DEVDDIR=	/etc/devd
-.if ${MK_ACPI} != "no"
-DEVD+=	asus.conf
-.endif
 
 .if ${MK_AUTOFS} != "no"
 CONFGROUPS+=		AUTOFS
@@ -27,11 +24,6 @@
 POWERPROFILE+=		power_profile.conf
 POWERPROFILEPACKAGE=	acpi
 
-CONFGROUPS+=		CONSOLE
-CONSOLEDIR=		${DEVDDIR}
-CONSOLE+=		moused.conf syscons.conf
-CONSOLEPACKAGE=		console-tools
-
 .if ${MK_BLUETOOTH} != "no"
 CONFGROUPS+=		BLUETOOTH
 BLUETOOTHDIR=		${DEVDDIR}
@@ -44,22 +36,6 @@
 HYPERVDIR=${DEVDDIR}
 HYPERV+=	hyperv.conf
 HYPERVPACKAGE=	hyperv-tools
-.endif
-
-CONFGROUPS+=		NVME
-NVMEDIR=		${DEVDDIR}
-NVME+=			nvmf.conf
-NVMEPACKAGE=		nvme-tools
-
-.if ${MK_SOUND} != "no"
-CONFGROUPS+=		SND
-SNDDIR=			${DEVDDIR}
-SND=			snd.conf
-SNDPACKAGE=		sound
-.endif
-
-.if ${MK_USB} != "no"
-DEVD+=	uath.conf ulpt.conf
 .endif
 
 .if ${MACHINE_ARCH} == "powerpc"
