diff -Naur src.orig/sbin/devd/Makefile src/sbin/devd/Makefile
--- src.orig/sbin/devd/Makefile	2025-11-28 04:15:53.000000000 +0400
+++ src/sbin/devd/Makefile	2025-12-27 13:39:11.709886000 +0400
@@ -6,9 +6,6 @@
 CONFS=	devd.conf
 DEVD=	devmatch.conf
 DEVDDIR=	/etc/devd
-.if ${MK_ACPI} != "no"
-DEVD+=	asus.conf
-.endif
 
 .if ${MK_AUTOFS} != "no"
 CONFGROUPS+=		AUTOFS
@@ -29,7 +26,7 @@
 
 CONFGROUPS+=		CONSOLE
 CONSOLEDIR=		${DEVDDIR}
-CONSOLE+=		moused.conf syscons.conf
+CONSOLE+=		moused.conf
 CONSOLEPACKAGE=		console-tools
 
 CONFGROUPS+=		SND
@@ -55,10 +52,6 @@
 NVMEDIR=		${DEVDDIR}
 NVME+=			nvmf.conf
 NVMEPACKAGE=		nvme-tools
-
-.if ${MK_USB} != "no"
-DEVD+=	uath.conf ulpt.conf
-.endif
 
 .if ${MACHINE_ARCH} == "powerpc"
 DEVD+=	apple.conf
