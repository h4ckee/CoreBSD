#!/bin/sh
#
# /etc/rc.resume: system resume script
#

source /etc/rc.conf

logger -t ACPI resume at $(date +'%Y%m%d %H:%M:%S')
if (( ${#services[@]} )); then
	for service in "${services[@]}"; do
		service="${service#@}"
		case "$service" in
		\!*) continue ;;
		anacron|bsdfan|ntpd) service "$service" restart ;;
		esac
	done
fi
